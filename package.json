{
  "name": "display-wizard-workspace",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "install:ci": "pnpm install --no-frozen-lockfile",
    "build:web": "pnpm -C ui/web build",
    "build:desktop": "pnpm -C ui/desktop build"
  }
}