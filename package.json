{
  "name": "vdisplay-wizard-workspace",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "install:ci": "pnpm install --no-frozen-lockfile",
    "build:web": "pnpm -C apps/web build",
    "build:desktop": "pnpm -C apps/desktop build"
  }
}